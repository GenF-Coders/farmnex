from fastapi import APIRouter
from app.core.responses import ok
router=APIRouter()
@router.get("")
async def rescue(): return ok([{ "id":8001,"crop_id":5,"crop_name":"Tomato","quantity":350,"unit":"kg","urgency":"high","days_to_spoilage":2,"offer_price":18,"location":"Pune","status":"active"}])
@router.post("")
async def create_rescue(): return ok({"id":8002,"status":"draft"},"Rescue listing created")
@router.get("/{rescue_id}")
async def get_rescue(rescue_id:int): return ok({"id":rescue_id,"crop_name":"Tomato","urgency":"high","status":"active"})
@router.patch("/{rescue_id}")
async def update_rescue(rescue_id:int): return ok({"id":rescue_id,"updated":True})
@router.delete("/{rescue_id}")
async def delete_rescue(rescue_id:int): return ok({"id":rescue_id,"deleted":True})
@router.post("/{rescue_id}/activate")
async def activate(rescue_id:int): return ok({"id":rescue_id,"status":"active"})
@router.post("/{rescue_id}/sold")
async def sold(rescue_id:int): return ok({"id":rescue_id,"status":"sold"})
@router.post("/{rescue_id}/cancel")
async def cancel(rescue_id:int): return ok({"id":rescue_id,"status":"cancelled"})
