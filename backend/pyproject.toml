[project]
name = "farmnex-backend"
version = "0.1.0"
description = "FarmNex FastAPI backend"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]",
    "uvicorn",
    "SQLAlchemy>=2.0",
    "asyncpg",
]